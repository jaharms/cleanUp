import React from "react";
import { Text, View } from "react-native";

interface IProps {}

export const Details = (props: IProps) => {
  return (
    <View>
      <Text>Hello</Text>
    </View>
  );
};
